<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <solid android:color="@color/color_search" />
    <stroke
        android:width="1dp"
        android:color="@color/color_search" />
    <corners android:radius="@dimen/_10sdp" />
</shape>